name: medicare
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  lottie: ^2.0.0 # Keep only one entry for lottie
  cupertino_icons: ^1.0.0  # Specify the version number for cupertino_icons
  firebase_core: ^3.10.1
  firebase_auth: ^5.4.1
  firebase_database: ^11.3.1 
  geolocator: ^9.0.2
  permission_handler: ^11.0.1
  http: ^1.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/medicine.json # Path to your animation file
    - assets/medicine login1.jpg
